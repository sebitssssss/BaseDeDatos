-- TRABAJO PRÁCTICO CARNICERÍA

-- 1A) Verdadero
-- 1B) Falso, se puede crear una tabla que carezca de Primary Key, aunque sea teóricamente incorrecto
-- 1C) Falso, se pueden usar más funciones, ej: HAVING
-- 1D) Falso, se puede usar más de una tabla si son unidas de alguna forma, pero no se puede unirlas con un FROM, es ambiguo

-- 2) La diferencia es que el WHERE filtra uno por uno sin agruparlos previamente, mientras que el HAVING filtra despues de haber agrupado. La diferencia es notoria cuando se lidia con muchos registros
